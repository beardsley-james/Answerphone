* RegEx business names so that I can just stick the name wherever. I could also just set a return string and make it way simpler
* setTimer instead of setInterval
* End of day report
* Acquisition of operators
* Client acquisition
* Jiggle number values to make it actually fun
* Figure out why they're not sticking with calls to completion
* Fix the event handler so that the stat line doesn't fuck it up (get rid of the stat line?)
* Maybe some kind of animation so it doesn't look so jumpy
* Why do the stupid op boxes flop around all over the place? Maybe fix that took
* Get some more names
